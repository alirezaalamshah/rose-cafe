# یادداشت انتقال — برای ادامه‌ی کار روی این پروژه در یک سیستم جدید

این فایل رو برای خودم (Claude) نوشتم تا وقتی این ریپازیتوری روی یک سیستم دیگه کلون بشه و اونجا کارو ادامه بدیم، سریع به context کامل پروژه برسم — بدون این‌که کاربر مجبور باشه دوباره از اول همه‌چیز رو توضیح بده.

**تاریخ نوشتن**: ۲۰۲۶/۰۹/۰۶ (۱۴۰۵/۰۶/۱۵ شمسی)
**آخرین کامیت روی این برنچ**: `7ffd52c` — برنچ `feature/snapp-courier-integration` (هنوز merge/deploy نشده)

---

## کاربر کیست

- به فارسی صحبت می‌کنه، همیشه پاسخ فارسی می‌خواد
- صاحب/توسعه‌دهنده‌ی یک کافه واقعی به اسم «رز کافه» در **دزفول** (نه تهران — این نکته چند بار قبلاً اشتباه شده، هرگز از تهران/ونک به‌عنوان placeholder استفاده نکن)
- ایمیلش: `Ali.Heev.clu@gmail.com`
- سایت زنده و در حال استفاده‌ست: **rccoffee.ir** — هر تغییری که مستقیم رو دیتای واقعی مشتری‌ها اثر بذاره باید با احتیاط انجام بشه

## این پروژه چیست

اپ کافه با پنل مشتری + پنل ادمین + پنل سرپرست سالن (waiter). React/Vite (دارک تم، RTL فارسی) + Django REST (MySQL روی هاست، محلی‌اش هم MySQL). دیپلوی روی cPanel اشتراکی (نه Docker/Liara — اون مهاجرت قدیمی‌تر کامل شده).

## قوانین کاری که کاربر صریحاً خواسته (خیلی مهم، رعایت نشدنش قبلاً مشکل ساز شده)

1. **من (Claude) هرگز مستقیم روی سرور/هاست زنده دستور اجرا نمی‌کنم** — همیشه دستورات رو می‌نویسم، کاربر خودش کپی/پیست و اجرا می‌کنه روی هاست. نه curl زدن به سایت زنده، نه rsync (روی هاست نصب نیست، از `cp` تک‌فایل استفاده می‌شه).
2. **قبل از هر push، هم تست بک‌اند (`python manage.py test`) هم بیلد فرانت (`npm run build`) باید سبز باشن.**
3. کامیت‌ها باید با این خط تموم بشن:
   ```
   Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
   ```
   و توضیحات PR با:
   ```
   🤖 Generated with [Claude Code](https://claude.com/claude-code)
   ```
4. کار روی `main` مستقیم نه — همیشه یک feature branch بساز.
5. وقتی چند مسیر طراحی داریم که تصمیمش واقعاً به کاربر بستگی داره (نه یک انتخاب فنی بدیهی)، از کاربر بپرس، حدس نزن.
6. اگه یک تغییر بزرگ/معماری‌محور بود (مثل کشف MyISAM/InnoDB روی هاست تولید قبلاً)، قبل از عمل، تأیید صریح بگیر.

## ابزارهای لازم روی سیستم جدید (قبل از شروع کار چک کن)

- **Node.js + npm** (فرانت React/Vite)
- **Python 3.12 + venv** (بک‌اند Django) — روی این سیستم `backend/venv/` بود ولی گیت‌ایگنور شده، باید دوباره ساخته بشه:
  ```bash
  cd backend
  python -m venv venv
  # ویندوز:
  venv\Scripts\activate
  pip install -r requirements.txt
  ```
- **MySQL محلی** — روی این سیستم یک نصب پرتابل (نه سرویس ویندوز) داشتیم:
  - مسیر باینری: `~/dev-tools/mysql-8.0.43-winx64/bin/mysqld.exe`
  - مسیر دیتا: `~/dev-tools/mysql-data`
  - فایل کانفیگ: `~/dev-tools/my.ini` (پورت **3307**، نه پیش‌فرض 3306!)
  - اجرا: `mysqld.exe --defaults-file="~/dev-tools/my.ini"` (پس‌زمینه)
  - **روی سیستم جدید احتمالاً این مسیر عیناً وجود نداره** — باید یا همین MySQL پرتابل رو نصب کنی، یا یک MySQL دیگه (سرویس ویندوز عادی، پورت 3306) نصب کنی و `backend/config/settings/dev.py` رو با `PORT`/مسیر جدید هماهنگ کنی.

## فایل‌های حساس که در گیت نیستن (باید دستی بسازی)

هیچ‌کدوم این‌ها commit نشدن (گیت‌ایگنور شدن، حاوی رمز/داده واقعی‌ان). کاربر باید مقادیر واقعی رو بده یا از سیستم قبلی کپی کنه:

- **`backend/.env`** — از روی `backend/.env.example` بساز. فیلدهای کلیدی:
  - `DJANGO_SETTINGS_MODULE=config.settings.dev`
  - `DATABASE_URL` — در این پروژه واقعاً استفاده نمی‌شه؛ `config/settings/dev.py` مستقیم `DATABASES` رو با مقادیر هاردکد (`NAME='cafe_db'`, `USER='cafe_user'`, `PASSWORD='Alireza.6683'`, `HOST='127.0.0.1'`) تنظیم می‌کنه. فقط پورت از `.env` قابل تغییره: `DB_PORT` (پیش‌فرض کد `3307`). اگه سیستم جدید MySQL روی پورت دیگه‌ای (مثلاً 3306 پیش‌فرض) داره، فقط `DB_PORT=3306` رو تو `.env` بذار — نیازی به دستکاری کد نیست. اگه user/pass/نام دیتابیس هم فرق داره، باید مستقیم `dev.py` رو ویرایش کنی.
  - `MELIPAYAMAK_*`, `ZARINPAL_*` — برای پیامک/پرداخت؛ برای کار محلی می‌تونن خالی/sandbox بمونن
  - `SNAPP_BOX_*` — تازه اضافه شده (این نشست)؛ کاربر هنوز client_id/secret واقعی نگرفته، فعلاً خالی بمونه مشکلی نیست (فقط ساختار کد تست می‌شه، نه اتصال واقعی)
  - `VAPID_*` — برای Push Notification؛ با `python manage.py generate_vapid_keys` می‌شه دوباره ساخت
- **`frontend/.env`** — از روی `frontend/.env.example` بساز؛ محتوای مثالش:
  ```
  VITE_API_BASE_URL=http://localhost:8000/api
  VITE_MEDIA_BASE_URL=
  ```
  (`VITE_MEDIA_BASE_URL` رو در dev خالی بذار تا Vite proxy کار کنه.)

## دیتابیس محلی

روی سیستم قبلی یک دیتابیس MySQL به اسم `cafe_db` بود که migration های کامل روش اجرا شده بود (احتمالاً با کمی داده‌ی تست). روی سیستم جدید:
```bash
cd backend
python manage.py migrate
python manage.py createsuperuser   # یا از طریق /api/auth/register مستقیم یک ادمین بساز
```
اگه کاربر یک دامپ SQL از دیتابیس محلی قبلی داد (فایل‌های `.sql`/`.sql.gz` تو `.gitignore` هستن پس تو گیت نیست، باید جدا منتقل بشه)، به‌جای migrate از صفر، همون رو import کن.

## معماری بک‌اند (Django apps)

```
backend/apps/
  accounts/       کاربر، آدرس (حالا با lat/lng!)، احراز هویت OTP+رمز، پروفایل ادمین کامل مشتری
  business/       تنظیمات کافه (ساعت کاری، اطلاعات، بنر) — الگوی CachedSingletonModel اینجا تعریف شده
  common/         کمک‌کننده‌های عمومی: local_day_range، haversine_distance_km، geocoding.py (پروکسی Nominatim)
  discounts/      کد تخفیف
  menu/           منو، دسته‌بندی، تنوع/افزودنی
  notifications/  Push (Web Push/VAPID)، پیامک (ملی‌پیامک)
  orders/         سفارش، آیتم سفارش — قلب پروژه
  payments/       زرین‌پال
  reservations/   رزرو میز
  reviews/        نظرات
  snapp/          یکپارچه‌سازی اسنپ‌باکس (پیک) — کاملاً جدید، این نشست ساخته شد
  staff_activity/ لاگ فعالیت کارکنان (StaffActionLog)
  wallet/         کیف‌پول مشتری + باشگاه مشتریان (کش‌بک)
```

## معماری فرانت‌اند

```
frontend/src/
  pages/
    admin/     پنل ادمین (سفارشات، منو، کاربران، داشبورد، تنظیمات، گزارش‌ها...)
    waiter/    پنل سرپرست سالن
    orders/    سبد خرید + لیست سفارشات مشتری
    profile/   پروفایل مشتری (آدرس‌ها اینجا مدیریت می‌شن)
    ...
  components/common/   کامپوننت‌های مشترک — Input، Modal، Button، و تازه‌ها:
    MapLocationPicker/     انتخاب پین روی نقشه (Leaflet)، با هشدار شعاع مجاز
    ReadOnlyMap/           نمایش فقط‌خواندنی یک نقطه (برای رهگیری پیک/نمایش آدرس ثبت‌شده)
    CourierStatusCard/     کارت وضعیت پیک (برای مشتری/ادمین/سرپرست سالن)
    AddressFormFields/     فرم مشترک آدرس (بین ProfilePage و CartPage) — نقشه + geocoding اینجاست
  api/         هر فایل معادل یک app بک‌انده (auth.js, orders.js, snapp.js, ...)
```

## وضعیت فعلی: فاز بزرگ اخیر — یکپارچه‌سازی اسنپ‌باکس (Snapp Box)

این کل نشست قبلی صرف این شد. خلاصه‌ی کامل در فایل حافظه‌ی من هست (`reference_snapp_api.md` — رو سیستم قبلی در `~/.claude/projects/.../memory/`، **این فایل با کلون‌کردن ریپو منتقل نمی‌شه، چون بیرون از ریپو و مخصوص دستگاهه**). خلاصه‌ی خیلی فشرده:

- API واقعی: **Snapp Box B2B** (`snapp-box.com`)، نه پنل سازمانی قدیمی
- معماری کامل کد نوشته و تست شده: ثبت/لغو/ارسال‌مجدد سفارش پیک، وبهوک، circuit breaker، آمار داشبورد، رهگیری زنده، دسته‌بندی واقعی پیک از API
- **بلاک‌کننده‌ی واقعی تست کامل**: کاربر هنوز `client_id`/`client_secret` واقعی اسنپ رو از پنل B2B نگرفته. بدون این فقط ساختار/mock تست شده، نه اتصال واقعی HTTP.
- یک سؤال باز: آیا اسنپ‌باکس API مدیریت شارژ اعتبار (کیف‌پول سازمانی) داره؟ — در مستنداتی که داریم پیدا نشد؛ کاربر قراره وقتی با پشتیبانی تماس گرفت بپرسه.
- **تصمیم صریح کاربر**: دیپلوی این فاز عمداً معلق مونده تا کلید برسه — چون سایت زنده‌ست و نمی‌خواد چیزی که تست واقعی نشده رو دیپلوی کنه. توالی توافق‌شده: کلید بگیر → تست واقعی در فضای دپلوی → بعد commit/push/deploy نهایی به main.

### گپ مهم که حل شد: آدرس مشتری حالا مختصات داره
- `Address` مدل `latitude`/`longitude` گرفت، **اجباری** شده (هم فرانت هم بک‌اند اعتبارسنجی می‌کنه)
- شعاع مجاز ثبت آدرس حول کافه (پیش‌فرض ۱۵ کیلومتر، قابل‌تنظیم در `SnappSettings.max_delivery_radius_km`) — آدرس دورتر از این رد می‌شه
- Geocoding دوطرفه با **Nominatim** (رایگان، بدون کلید، ولی سقف نرخ ۱ req/s — عمداً فقط با کلیک صریح دکمه، نه suggestion زنده)

## قبل از شروع کار روی سیستم جدید، این‌ها رو چک کن

1. `git log --oneline -5` بزن، مطمئن شو رو `feature/snapp-courier-integration` هستی (یا هرچی که بعداً merge/فرق کرد)
2. `python manage.py test` (بک‌اند) و `npm run build` (فرانت) رو یک‌بار زودتر اجرا کن تا مطمئن شی محیط درست ساخته شده، قبل از این‌که کاربر کار جدید بخواد
3. اگه کاربر گفت «ادامه بدیم»، بدون توضیح مجدد از طرف کاربر، برو سراغ:
   - اگه کلید اسنپ رو گرفته → تست واقعی endpoint ها، بعد کمک به دیپلوی
   - اگه هنوز نگرفته → بقیه‌ی TODO های کوچیک‌تر (فایل صوتی `courier-alert.mp3` که قرار بود ضبط کنه، هنوز اضافه نشده به `frontend/public/sounds/`)

## نکات فنی مهم که چند بار قبلاً باعث باگ شدن (تکرار نشه)

- `CachedSingletonModel` (کش فرآیندی) بین تست‌ها پاک نمی‌شه خودکار — همیشه `cache.clear()` تو `setUp()` تست‌های جدید که `SnappSettings`/`LoyaltySettings`/`CafeInfo` و مشابه رو لمس می‌کنن
- Vite پورت **3000** هست، نه پیش‌فرض 5173 — تو `vite.config.js` تنظیم شده
- Leaflet داخل Modal بدون `isolation: isolate` روی CSS جعبه‌ی نقشه، کنترل‌های زوم از مودال بیرون می‌زنن (رفع شده، ولی اگه یک نقشه‌ی جدید دیگه اضافه شد این الگو رو رعایت کن)
- هر migration جدید Django باید هم `makemigrations` هم `migrate` بشه، بعد اگه به هاست دیپلوی شد، کاربر خودش رو هاست `migrate` می‌زنه (من مستقیم نمی‌زنم چون سایت زنده‌ست)

## دستور شروع سریع (بعد از env/venv/db آماده)

```bash
# بک‌اند
cd backend
venv\Scripts\activate   # ویندوز
python manage.py test   # باید سبز باشه
python manage.py runserver

# فرانت (ترمینال جدا)
cd frontend
npm install
npm run build   # باید تمیز باشه
npm run dev     # سرور dev، پورت 3000
```
